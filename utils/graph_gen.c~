/*
 * filename: gen.c 
 * author: onesuper
 * email: onesupperclark@gmail.com
 *
 * some functions used for generating graphs
 */

/*
 * it gets a name of a graph file and read the file
 * line-by-line to generate the graph
 */ 
void graph_gen(char** filename) 
{
	 fp = fopen(filemane, "r");
	 if (!fp) {
		  printf("Cannot read the graph file.\n");
		  return;
	 }
	 
	 
}

